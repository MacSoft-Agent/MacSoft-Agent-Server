# Paths and storage

Customer-owned values. Use managed ProgramData locations in packaged mode.

- Workflow evidence root: `NOT_CONFIGURED`
- Payment archive root: `NOT_CONFIGURED`
- Receiving archive root: `NOT_CONFIGURED`

Only archive validated source files. Preserve the original filename as metadata, generate a collision-safe stored name, and record SHA-256, byte size, MIME type, source channel, and Case ID.
